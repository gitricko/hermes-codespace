Developer/engineer on Hermes-CodeSpace (repo owner intricko). Wants concise practical responses, working artifacts, honesty w/ caveats. PR-to-main + merge gate; conservative CI (infra full build vs docs/runtime lint). Root-cause fixes (don't weaken tests). Wiki=reference vs skill=procedural; keeps skill+wiki+mnemon in sync. Loads relevant skills proactively. Dislikes convoluted/defensive boot logic — prefers spare, single-responsibility design. Wants infra invariants AUTO-VERIFIED in CI, not just documented. Understands Codespace idle = client activity/billing implications. Wants proposals iterated IN lavish (not MD files). Wants dynamic multi-session lavish-axi (slot registry keyed by Hermes session ID). Prefers port scheme: engine 43XX ↔ public 99XX (matching suffixes). Mnemon: mnemon_remember + merge to seed.json for persistence. CI: content-only → lint-check only; infrastructure → full build.
§
Use Mnemon (mnemon_remember / mnemon_recall) as primary memory provider instead of standard memory() tool. Mnemon has no char limit. Only fall back to memory() for structured preference data (target=user or target=memory). Mnemon insights should be merged into .devcontainer/mnemon/seed.json for cross-spawn persistence; validate with mnemon import --dry-run.