name: Dev Container CI

on:
  push:
    branches: [main, readme]
  pull_request:
    branches: [main]

jobs:
  test-devcontainer:
    name: Build & Health Check
    runs-on: ubuntu-latest
    timeout-minutes: 35
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Build dev container & run health check
        uses: devcontainers/ci@v0.3
        with:
          imageName: hermes-codespace-test-${{ github.run_id }}
          push: never
          runCmd: |
            set -e
            echo "=== Starting all services via start-hermes.sh ==="
            bash .devcontainer/start-hermes.sh

            echo "=== Running health check ==="
            bash .devcontainer/self-check.sh